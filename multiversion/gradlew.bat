@rem Wrapper batch script intentionally omitted for now.
@rem Use the repo-root Gradle wrapper to build the multiversion project:
@rem   cd multiversion
@rem   ..\gradlew <task>
@echo off
echo Use ..\gradlew from repo root.

